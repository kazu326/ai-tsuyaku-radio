# Jev実験 検証記録

実施日：2026-09-19。対象：完成済みEpisode 004の確定セリフ。

## 結論・ステータス（2026-09-23）

**実験終了。Jevを既存のAI通訳ラジオ制作フローには導入しない。** 12単位・36判断の結果は検証記録として保存し、182単位への拡大や制作フローへの接続は行わない。Jev関連の追加実装も停止する。

今回試した視覚補助・セクション切替・注意回復の判断では、Jevを追加しても後段のAstra/Codexによる制作判断と人間の最終確認を省略できず、工程が一段増えるためである。この結果はJev自体が不適切という意味ではなく、あらかじめ定義された選択肢について状態やコンテキストから高速・低コストに分岐判断する用途には可能性がある。たとえば、ユーザー状態に応じたUI/UX変更、会話状態に応じた処理分岐、大量の曖昧なif判断、confidenceに応じた自動処理・上位モデル・人間へのルーティングが候補となる。

AI通訳ラジオで再検討するのは、制作の自動化がさらに進み、Jevの判断によって後工程そのものを省略できる明確な用途が生じた場合に限る。今回の記録だけでは人間の編集判断との一致率や制作上の有効性は確定しない。

## 最新結果：12/12単位・36判断を取得済み

ユーザーからチャージ完了の連絡後、`20260919T132314811Z-a30bfb5a` で未取得7単位だけを2秒間隔で評価し、7/7成功。既存5単位の再送なし。全入力・質問のdataset IDと原本4ファイルのSHA-256一致を確認。

統合結果：[12単位の比較表](runs/ep004-12-unit-review-20260919/comparison.md) / [CSV](runs/ep004-12-unit-review-20260919/comparison.csv) / [原結果](runs/ep004-12-unit-review-20260919/results.json)。実行ごとの履歴は残したまま、成功応答だけをID順に統合。

| 項目 | 12単位の結果 |
|---|---|
| 視覚補助 | NONE 9 / TEXT 0 / IMAGE 0 / DIAGRAM 2 / COMPARISON 1 |
| セクション切替 | YES 3 / NO 9 |
| 注意回復 | YES 8 / NO 4 |
| 入力token | 16,144 |
| 出力token | 1,458 |
| 成功応答の処理時間合計 | 6,442ms |
| 処理時間中央値 / 範囲 | 526ms / 410〜638ms |
| Gateway報告 cost | 0 USD |
| Gateway報告 marketCost | 0.000678048 USD |
| Gateway報告 gatewayCost / surchargeCost | ともに0 USD |

今回のZDR削除後テストでGatewayへ送信したのは15回（成功12、429が3）。429のusage/costは未返却。ここでの金額は成功応答のmetadataを集計した報告値であり、ダッシュボードの最終請求額を照合したものではない。処理時間は通信・SDK処理を含み、送信間隔の待ち時間は除外。

観察点：

- c116「もし単純な国同士の勝負なら…」はCOMPARISON 68%、confidence 0.61。
- c166「外国との協力を続けながら…製造ルートも増やす」はDIAGRAM 73%、confidence 0.66。
- c149「AIを作り続けられる能力と…選択肢」はDIAGRAM 43%に対しTEXT 31%、confidence 0.28。候補間で迷っている。
- c067の注意回復はYES 52% / confidence 0.04、c133はYES 51% / confidence 0.01。分類だけを採用すると曖昧さを落とす。
- c083の問いかけはセクション切替YES 92% / confidence 0.84、注意回復YES 93% / confidence 0.86。

人間ラベルは未記入で、精度・一致率は未評価。既存cueは比較資料に留める。12単位から全編・他Episodeへの有効性はまだ一般化しない。182単位への拡大と本番フローへの接続は行っていない。

## 追記：ZDR削除後の12単位テスト

ユーザー承認により、実験内のZDR指定だけを削除。質問・モデル・SDK・入力・サンプルの変更なし。初回スナップショットとdataset ID一致。403をAUTHへ一律変換する記録も、401=AUTH / 403=FORBIDDENに修正。

全選択肢の確率・confidence・実測時間・token数・Gatewayのコスト返却値をMarkdown/CSVへ追加。オフライン13テストと型検査に合格。既存制作ファイルの変更なし。

今回のrun：`runs/20260919T130843167Z-18aed0be/`。

- 最初の5単位で15判断を取得。6件目（ep004-c083）は429。後続6単位は未送信。
- 新しいrun `20260919T131008509Z-dff14f9f` で未完了7単位の15秒間隔実行を試みたが、最初の1件で再び429になり停止。
- `20260919T131126782Z-3efbeb04` で同じ未完了1単位の制限内容を確認。無料枠のモデル別レート制限と返り、Retry-Afterなし、provider試行0件。診断応答は秘密情報を除いて同runへ保存。
- ここまでGateway送信8回（成功5、429が3）。成功済み5単位の再送なし。182単位の全件評価なし。

取得済み5単位の合計は、入力6,652 token、出力605 token、処理時間2,799ms、中央値540ms。Gateway返却値の合計は `cost=0 USD` / `marketCost=0.000279384 USD` / `gatewayCost=0` / `surchargeCost=0`。失敗応答にusage/costはなく、未取得分を0と見なして総請求額を断定しない。

比較表は各runの `comparison.md`、表計算用は `comparison.csv`。この中断時点では5/12取得であり、残り7単位の判断は推測で埋めず、後続runで取得した。

以下はZDRを指定していた初回の履歴です。認証・請求設定とした診断は、後続の最小リクエスト比較により **HobbyプランでのZDR機能制限** と特定されました。

## 実装・境界

- `experiments/jev/` のみ追加。既存の制作フロー、Remotion、Web、台本、音声、字幕、既存package/lockfile、`.env.local` の変更なし。
- `git diff --exit-code` 成功。Git未追跡ファイルは実験フォルダの追加のみ。
- 原本4ファイルのSHA-256を入力に記録し、実APIテストの前後で一致を確認。
- キー、依存フォルダ、途中の診断runはGit対象外。確定した12単位の統合結果のみGitに保存し、APIキーは出力ファイルへ保存しない。
- Vercelの公開モデル一覧と公式ドキュメント、インストールしたAI SDKのソースを確認済み。AI SDK 7.0.105固定、Gateway provider 4.0.85（lockfile固定）。

## オフライン

`node --test test/*.test.mjs`：12件成功。

1. 実際の完成セリフ182件の原文・時刻を保持。
2. 空・壊れた・順序不正の入力を拒否し、原本にある字幕区間の重なりは保持。
3. 対象の前3・後1単位を取得し、実編集や人間ラベルをJev入力へ混入させない。
4. スライド区間の重なりとSEのFPS変換を検証。
5. 12件の抽出が全編に分散し、重複・範囲外指定を拒否。
6. confidenceと選択確率を分離し、未返却confidenceはnull。
7. 不正分類・確率・confidenceを拒否。
8. 実際のAI SDK Gateway adapterをモックHTTP応答で検証（3質問・ZDR・返却構造・usage）。
9. キー未設定を拒否、429で自動再試行しない。
10. 送信前に入力を記録、エラーを秘匿化、最初の失敗で残りを停止。
11. 未記入をNOと見なさず、一致・不一致・未判定を分離。
12. ディレクトリトラバーサル・任意出力先指定を拒否。

`node node_modules/typescript/bin/tsc --noEmit`：成功。型検査対象は公式APIに接続する `provider.ts` と質問定義 `questions.ts`。その他のCLI・比較・保存コードはNodeテストで検証。

`prepare`：182単位と人間ラベル記入用ファイルを作成。

dry run：全編から12単位、計36質問と表示。API送信なし。

`compare`：保存結果と未記入ラベルから比較表を生成。全項目の人間ラベル数0・一致率「未評価」。

## 実API

run：`20260919T093152376Z-7d18f270`

出力：`runs/20260919T093152376Z-7d18f270/`（当時の診断run。ローカル、Git対象外）

| 項目 | 結果 |
|---|---|
| 計画 | 全編から12単位 × 3質問 |
| 通常実行での送信 | 最初の1単位だけ |
| Gateway応答 | HTTP 403 |
| 残り11単位 | `skipped`。送信していない |
| Jev判断取得 | 0件 |
| 原本ハッシュ | 実行前後で一致 |
| 追加診断 | 同じ最初の1単位で1回確認、再び403。キー・本文を出力せずエラー種別のみ確認 |

したがって、Gatewayへの実リクエストは合計2回、成功0回。認証・請求設定に関するエラー語句が検出されましたが、キーの無効化・権限・残高のどれが根本原因かは未確定。APIキーが設定され、明らかなプレースホルダーや空白混入でないことまでは確認しました。

**実際のJevの判断品質・confidence分布・人間との一致率は、まだ評価できていません。** テスト用の固定応答は通信契約の検証にだけ使用し、実結果には混ぜていません。

## 当時の予定（実験終了により未実施）

当時はVercel AI Gatewayのキー・モデル利用権限・請求設定を確認後、新しい `prepare` と12単位の `evaluate --live` を予定していました。その後、12単位の結果取得までは完了しましたが、人間ラベルの記入と `compare` による一致率の検証は実施していません。今後の追加実験は予定していません。

実編集cueは承認済みの制作記録として参照できますが、レンダリング後の人間編集や演出意図を表す正解データではありません。人間による確認を経ずに精度評価や本番処理へ進めません。
