# 第2話 本編制作

状態：背景デコードを第2話のみOffthreadVideoへ変更し、本番Compositionの80〜155秒（75秒）をConcurrency 1で診断完走。全編再レンダリング前で停止。全編MP4未完成・人間による全編視聴未実施。

## 背景デコード修正と75秒診断（最新）

初回全編は2971付近でb2取得タイムアウト。独立12秒診断ではVideo経路のハードカット・クロスフェードともa7内0.167秒で停止し、OffthreadVideo経路は両方成功。ユーザー承認により第2話の背景のみ経路を変更。

- BackgroundPlaylistの順序生成・Sequence時刻・9フレームのクロスフェード・暗幕は維持。任意のvideoComponentを受け取るようにし、第2話だけNativeBackgroundVideoを指定。既存Compositionでは従来Videoを使用する。
- NativeBackgroundVideoはOffthreadVideoを使用し、ループ可能素材は実測動画尺を用いたLoopで繰り返す。背景素材自体・Registryは未変更。音声・字幕・V03補正・猫再生・本編尺も未変更。
- 診断出力：out/episode2-native-75s-c1.mp4。フレーム2400〜4649、2250フレーム。映像75.000秒、コンテナ75.050667秒、67,613,879 bytes。
- a1→b2（89.2秒）、b2→a7（98.9秒）、a7→c7（119.6秒）、c7→a5（137.3秒）の4切替を完走。タイムアウト0。全出力の映像・音声デコードエラー0。
- 冒頭・切替中・末尾の抜粋で背景／字幕／猫丸小窓を確認。音声2→3の切替を含む音声波形サンプルを照合し、約43msの一定差を観測、確認点で増大するズレなし。全編聴取承認ではない。音声処理は変更していない。
- 型・Lintエラー0、既存警告3件。修正前保護ファイルとの差分はsrc/Composition.tsxとsrc/episode2/Episode2Main.tsxの背景経路接続のみ（この制作記録更新前に照合）。新規NativeBackgroundVideo.tsxとbackground-video-metadata.jsonを追加。
- タイムアウト延長・再エンコード・素材置換・全編再レンダリングなし。診断詳細はout/episode2-native-75s-report.json。

## 第2回Alignmentと本編実装（最新）

- ユーザーがspoken-text-01〜05をAlignment本文として採用・送信承認。音声ごとに1回、計5回送信し、alignment-r02-01.json〜05.jsonと各.meta.jsonへ保存。初回のalignment-01〜05は履歴として保持。
- 全5本で本文全文・音声／本文／応答ハッシュ一致。時刻逆転0、音声尺外0。同一開始時刻の文字数は最大1（句読点・空白を除く）。各音声を10分割したすべての区間へ文字が分布し、末尾はSTT発話終端と一致。異常な終端長文集中なし。機械検査は合格、全編聴取による同期評価は別途。詳細：alignment-r02-verification.json。
- src/episode2/Episode2Main.tsxを新設しRootへ追加登録。既存5件を含め6件すべて読み込み成功。
- 1280×720・30fps・13945フレーム（464.833333秒）。音声を番号順に等速で配置。各尺をフレームへ切り上げるため、合計に約89msの丸め差あり。追加の演出用の間・末尾余韻は設けていない。
- 字幕は97区間。句読点を優先して区切り、2行に収まらない長文は既存の読点位置で分割。本文の文字は保持。元時刻をcaptions-unshifted.jsonに保存し、開始のみmax(0,startMs-300)を1回適用。終了時刻維持。29箇所の重なりはV03と同じ次字幕優先。
- 実フォント・幅842pxで71区間が1行、26区間が意味の区切りで2行。文字サイズ・黒マスク・位置・色等は既存SubtitleBoxをそのまま使用。caption-layout.jsonは表示改行のみ。layout-verification.jsonで97区間すべて84px以内・各行842px以内を確認。
- 猫は左20px・下20px・200×200pxの丸小窓。cat200.mp4の0秒から等速、無音・非ループ、本編末尾で終了。冒頭プレビューで背景・字幕との配置を確認。
- Opening・今日の話はこちら・Ending動画・画像差し込みなし。既存BackgroundPlaylistを変更せず使用。
- npm run lint成功：TypeScriptエラー0、ESLintエラー0・既存objectFit警告3件。既存保護対象の差分はRootへの追加登録のみ。素材・旧字幕・初回／STTキャッシュ・採用本文・package-lockは保持。
- 再生成用scripts/build-episode2-captions.mjsはネットワークを使わず、r02の補正前時刻から常に生成する。既存captions.jsonを補正入力にしないため二重補正を防ぐ。タイムラインや表示改行を変更する場合は音声と字幕の開始位置を共に確認する。

以下は取得前・文字起こし時点の経緯として保持する。

## 音声由来の本文（2026-09-10）

文字起こし後のユーザー指定：モデル番号は「GPT 5.6」、数量は「14倍」「1秒当たり最大750トークン」など算用数字を基本とする。spoken-text全5本の人数・回数・寸法・個数も同様に変更。「一方」「一斉」「一部」等の熟語は保持。音声・STT生応答・既存Alignmentは変更せず、表記修正をstt-verification.jsonに記録した。

ユーザー明示承認後、Scribe v2へ音声を1本ずつ計5回送信。参照原稿は送信せず、日本語・単語時刻を指定。追加インストールなし。stt-01.json〜05.jsonはAPI生応答、各.meta.jsonは入力ハッシュ・設定・応答ハッシュ。spoken-text-01.txt〜05.txtは字幕本文候補。

| 音声 | 対応する参考原稿範囲 | STT発話範囲（秒） |
|---|---|---|
| １.mp3 | script-01前半：導入 | 0.159〜72.379 |
| ２.mp3 | script-01後半：CPU・GPU | 0.179〜81.459 |
| 3.mp3 | script-02：GPU間のデータ移動 | 0.199〜82.779 |
| 4.mp3 | script-03：巨大チップ | 0.160〜124.940 |
| 5.mp3 | script-04：故障回避・UltraFast | 0.140〜102.720 |

script-05の解釈・まとめ・次回予告に対応する内容は5本の認識結果にない。補完しない。音声4の画像に言及する発話もそのまま保持し、画像挿入は今回の対象外を維持する。

修正は01の4箇所・02の2箇所の英字綴りCelebras→Cerebrasのみ（固有名詞の明らかな認識誤り）。その他は文ごとの改行だけ。漢数字・カタカナ・言い回しを原稿へ合わせて変更していない。修正と検査詳細はstt-verification.json。

全5本で入力・応答ハッシュ一致、単語時刻の逆転・音声尺外・長文ゼロ長集中なし。冒頭から末尾までの認識文を読み、参照原稿の章境界と比較した。ただし独立した全編聴取による逐語照合は未実施で、ASRによる取りこぼしがないことまで保証する検査ではない。

既存166ファイルのハッシュを文書更新前に照合し変更なし。初回異常Alignment、元原稿、音声、V01／V02／V03を保持。Forced Alignment再送信・V03補正・Composition実装は実行していない。

## 第2話だけの例外運用（ユーザー指示）

音声生成時の最終本文を復元できないため、public/Episode2/の完成音声1〜5を正とする。script-01.txt〜05.txtは参考資料として保持し、不一致時は音声を優先する。第3話以降の標準フローは変更しない。

音声ごとに文字起こしし、spoken-text-01.txt〜05.txtを新規保存する予定。要約・書き換え・補完は禁止。明らかな認識誤りの修正、句読点、自然な文区切りのみ許容する。生の認識結果と修正記録も分けて保持する。

まず文字起こし5本と各音声との対応範囲の確認を行い、Forced Alignment再送信前にユーザーへ報告する。確認後の再取得は第2回と分かる別名で保存し、初回の異常結果も履歴として残す。正常な同期分布を確認してからV03補正・実装へ進む。

方法確認：ローカルのfaster-whisper関連フォルダとPython実体は存在するが、out/whisper-modelsはCACHEDIR.TAGのみでモデル本体がない。既存align-audio.pyはモデルがなければダウンロードするため実行していない。ElevenLabs Speech-to-Text APIを既存Node.jsから呼ぶ方法ならSDK追加は不要だが、音声5本の外部送信が必要。今回の文字起こし送信はまだ実行していない。

## 入力の対応

ユーザー指定の番号順を採用。発話内容との一致はAlignment取得後に検査する。

| 採用原稿 | 音声 | 音声尺（秒） | Alignment用本文 |
|---|---|---:|---|
| script-01.txt | public/Episode2/１.mp3 | 72.437500 | alignment-text-01.txt |
| script-02.txt | public/Episode2/２.mp3 | 81.554250 | alignment-text-02.txt |
| script-03.txt | public/Episode2/3.mp3 | 82.834250 | alignment-text-03.txt |
| script-04.txt | public/Episode2/4.mp3 | 125.152625 | alignment-text-04.txt |
| script-05.txt | public/Episode2/5.mp3 | 102.765688 | alignment-text-05.txt |

原稿は保持。Alignment用本文では角括弧の演出タグ、画像指示、独立した章番号、区切り線だけを除去し、発話本文の表現は維持した。script-01内の章番号1・2はファイル対応番号と区別する。script-05の「まとめ・エンディング」等の本文は保持し、外付けEnding動画の除外と混同しない。

予定保存先：alignment-01.json〜alignment-05.jsonと各.meta.json。既存のscripts/align-captions.mjsを利用し、音声・本文・応答のSHA-256でキャッシュを検証する。90秒テストの結果は変更しない。

## 本編仕様

- Composition ID：AI-Radio-Episode2-Main-v01。1280×720、30fps。
- 音声1〜5、既存背景、字幕、左下の猫のみ。Opening・今日の話はこちら・Ending動画・画像差し込みなし。
- V03字幕方式：本編時刻へ換算後、開始のみmax(0,startMs-300)。終了維持。補正は一度だけ。重なりでは次字幕優先。
- 既存SubtitleBoxの外観を再利用し、黒マスクは本編全体で常時表示。字幕テキストのみ切替。
- 猫はpublic/cat200.mp4の0秒から等速再生。本編末尾で終了。ループなし。
- 猫の元画像を実物確認済み。円形の絵柄と黒い四隅があるため丸くクリップする。左20px・下20px・200×200pxを採用予定。字幕位置は変更しない。合成画面での確認は未実施。
- 音声区間と画面切替の時刻を個別管理し、後から間・画像・背景を調整できる構成とする。

## 進行状況

2026-09-10：対象5組のElevenLabs送信についてユーザーの追加明示承認を受領し、APIを音声ごとに1回、計5回実行。alignment-01.json〜05.jsonと各.meta.jsonを保存。全5組の音声・本文・応答SHA-256がメタデータと一致。詳細はalignment-verification.json。

取得結果には長い本文が同一時刻・長さ0へ集中する異常がある。01はGPU説明からの589文字（改行含む）が72.399秒、03は225文字が81.599秒、04は346文字が123.170秒へ集中。02・05にも長いゼロ長区間がある。句読点や改行だけのゼロ長とは異なるため、この結果を正常な字幕同期として採用しない。

Forced Alignmentは指定本文の時刻推定であり、独立した文字起こしではない。音声内容との一致は確認できていない。特にscript-01.txtは章1と章2の本文を含むため、原稿の分割と音声ファイルの対応を再確認する必要がある。自動的な本文の入れ替え・削除や追加API送信はしない。

ユーザー指示の「問題なければ実装」の条件を満たさないため、V03補正・字幕生成・Composition実装・読み込み確認を停止。既存保護対象32ファイルのハッシュに変更なし。取得結果は比較証跡として保持し、再取得が必要になった場合も上書きしない。
